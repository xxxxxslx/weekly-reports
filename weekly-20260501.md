# 学习周报（20260501）

# time包
## 一、核心时间单位
| 常量 | 含义 |
|------|------|
| time.Nanosecond | 纳秒 |
| time.Microsecond | 微秒 |
| time.Millisecond | 毫秒 |
| time.Second | 秒 |
| time.Minute | 分钟 |
| time.Hour | 小时 |

## 二、获取时间基础信息
| 方法 | 作用 |
|------|------|
| time.Now() | 获取当前本地时间 |
| t.Year() | 获取年份 |
| t.Month() | 获取月份 |
| t.Day() | 获取日期 |
| t.Hour() | 获取小时 |
| t.Minute() | 获取分钟 |
| t.Second() | 获取秒 |
| t.Nanosecond() | 获取纳秒 |
| t.Weekday() | 获取星期几 |
| t.YearDay() | 获取一年中第几天 |
| t.Date() | 一次性返回 年、月、日 |
| t.Clock() | 一次性返回 时、分、秒 |

## 三、时间格式化与解析
| 方法 | 作用 |
|------|------|
| t.Format(layout) | 时间转字符串 |
| time.Parse(layout, str) | 字符串转时间对象 |
| 固定模板 | `2006-01-02 15:04:05`（Go专属生日时间） |

## 四、时间戳互转
| 方法 | 作用 |
|------|------|
| t.Unix() | 秒级时间戳 |
| t.UnixMilli() | 毫秒级时间戳 |
| t.UnixMicro() | 微秒级时间戳 |
| t.UnixNano() | 纳秒级时间戳 |
| time.Unix(sec, nsec) | 时间戳转为 time.Time |

## 五、时间加减与差值
| 方法 | 作用 |
|------|------|
| t.Add(d Duration) | 时间加减，正数加、负数减 |
| t.Sub(t2) | 计算两个时间间隔，返回 Duration |
| diff.Hours() | 时间差换算成小时 |
| diff.Minutes() | 时间差换算成分钟 |
| diff.Seconds() | 时间差换算成秒 |

## 六、时间比较
| 方法 | 作用 |
|------|------|
| t.Before(t2) | 判断t是否在t2之前 |
| t.After(t2) | 判断t是否在t2之后 |
| t.Equal(t2) | 判断两个时间是否相等 |

## 七、构造时间与时区
| 方法 | 作用 |
|------|------|
| time.Date() | 手动构造指定年月日时分秒时间 |
| t.Local() | 转为本地时间 |
| t.UTC() | 转为UTC标准时间 |

## 八、工具方法
| 方法 | 作用 |
|------|------|
| time.Sleep(d) | 程序休眠、延时 |

注意：格式化**固定模板 2006-01-02 15:04:05**，不能乱改数字。


---

# Go 指针 + new / make 
## 一、指针基础概念表格
| 符号 | 名称 | 作用 |
|------|------|------|
| `&变量` | 取地址符 | 获取变量的内存地址 |
| `*指针` | 解引用符 | 通过地址读写原变量的值 |
| `*T` | 指针类型 | 指向类型 T 的指针，存的是地址 |

## 二、new 与 make 核心区别表格
| 对比项 | new | make |
|-------|-----|------|
| 适用类型 | 所有基础类型、结构体 | 仅 **slice、map、channel** 三种 |
| 返回值 | 返回 **指针 *T** | 返回 **类型本身**（不是指针） |
| 内存初始化 | 分配内存，赋**零值** | 分配内存，初始化**可直接使用** |
| 是否能直接用 | 不能，需 `*p` 解引用 | 可以直接使用 |
| 用法示例 | `p := new(int)` | `s := make([]int,3,5)` |

## 三、指针常用方法
| 用法 | 说明 |
|------|------|
| 基础指针 | 用 `&` 取地址，`*` 改原值 |
| 函数传指针 | 函数内可修改外部原变量 |
| 结构体指针 | 避免结构体拷贝，直接修改成员 |
| nil 指针 | 未赋值指针默认是 `nil`，不能解引用 |

---




