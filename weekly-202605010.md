# 学习周报（20260510）


## 一、结构体是什么
**结构体（struct）** 是自定义复合类型，可以把**不同类型变量**封装在一起，类似面向对象的「类」。

---

## 二、结构体基础语法
### 1. 定义结构体
```go
type 结构体名 struct {
    字段1 类型
    字段2 类型
    ...
}
```

示例：
```go
type Student struct {
    Name string
    Age  int
    Sex  string
}
```

### 2. 初始化结构体
- 顺序初始化
- 键值初始化
- 匿名结构体
- 结构体指针

---

## 三、结构体核心知识点（表格版）
| 知识点 | 说明 |
|--------|------|
| 结构体定义 | `type 名字 struct{ 字段 }` |
| 实例化 | 声明变量、直接赋值 |
| 字段访问 | `结构体变量.字段名` |
| 结构体赋值 | 整体赋值、逐个字段赋值 |
| 结构体传参 | 值传递（默认拷贝） |
| 结构体指针传参 | 引用传递，可修改原结构体、节省内存 |
| 匿名字段 | 不写字段名，只写类型 |
| 嵌套结构体 | 结构体里包含另一个结构体 |
| 方法绑定 | 给结构体绑定方法（值接收者 / 指针接收者） |

---

## 四、值接收者 vs 指针接收者（重点）
| 接收者类型 | 写法 | 特点 |
|-----------|------|------|
| 值接收者 | `func (s Student) 方法()` | 拷贝一份，**改不动原结构体** |
| 指针接收者 | `func (s *Student) 方法()` | 操作原结构体，**可以修改字段**，省内存 |

---

## 五、示例
```go
package main

import "fmt"

// 1. 定义结构体
type Student struct {
	Name string
	Age  int
	Sex  string
}

// 2. 值接收者方法（不能改原数据）
func (s Student) showInfo() {
	fmt.Printf("姓名：%s 年龄：%d 性别：%s\n", s.Name, s.Age, s.Sex)
}

// 3. 指针接收者方法（可以改原数据）
func (s *Student) setAge(age int) {
	s.Age = age
}

// 4. 嵌套结构体
type Class struct {
	ClassName string
	Stu       Student // 嵌套结构体
}

func main() {
	// 方式1：逐个赋值
	var s1 Student
	s1.Name = "小明"
	s1.Age = 18
	s1.Sex = "男"
	s1.showInfo()

	// 方式2：键值初始化
	s2 := Student{
		Name: "小红",
		Age:  17,
		Sex:  "女",
	}
	s2.showInfo()

	// 方式3：顺序初始化
	s3 := Student{"小李", 19, "男"}
	s3.showInfo()

	// 方式4：结构体指针
	sp := &Student{"小张", 20, "男"}
	sp.showInfo()
	// 指针修改字段
	sp.setAge(22)
	fmt.Println("修改后年龄：", sp.Age)

	// 方式5：嵌套结构体
	c := Class{
		ClassName: "高三1班",
		Stu:       Student{"小王", 18, "男"},
	}
	fmt.Println("班级：", c.ClassName)
	fmt.Println("学生信息：", c.Stu)

	// 方式6：匿名结构体
	anon := struct {
		ID   int
		Addr string
	}{101, "北京"}
	fmt.Println("匿名结构体：", anon)
}
```

---

## 六、总结
1. struct 是**自定义复合类型**，封装不同类型字段；
2. 访问字段用 `.`；
3. 默认**值传递**，会拷贝，大数据建议用**结构体指针**；
4. **值接收者**不能修改原结构体，**指针接收者**可以修改；
5. 支持**嵌套结构体、匿名字段、匿名结构体**；
6. 结合指针使用，既能改原数据又能节省内存。

