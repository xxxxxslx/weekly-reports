# 学习周报（20260517）

---
## Go 结构体 ↔ JSON 互相转换
| 知识点 | 说明 |
|--------|------|
| 包 | `encoding/json` |
| 结构体 → JSON | `json.Marshal(结构体变量)` |
| JSON → 结构体 | `json.Unmarshal(JSON字节, &目标结构体)` |
| 字段标签 | `json:"key"` 定义JSON键名 |
| 忽略字段 | `json:"-"` |
| 空值忽略 | `json:"name,omitempty"` |
| 首字母要求 | **结构体字段必须大写（导出）**，否则无法序列化 |




## 注意点
1. **结构体字段必须首字母大写**（导出），否则 json 包无法访问
2. 标签 `json:"name"` 用来指定 JSON 里的 key
3. `Marshal` → 结构体转 JSON
4. `Unmarshal` → JSON 转结构体 **必须传指针**
5. 错误一定要处理


## 常用标签
| 标签写法 | 作用 |
|---------|------|
| `json:"name"` | 指定 JSON key |
| `json:"-"` | 忽略该字段，不参与转换 |
| `json:"age,omitempty"` | 空值不输出 |
| `json:"birthday,string"` | 强制转字符串 |

---

## Go Modules 
Go Modules 是 Go **1.11+ 官方依赖管理工具**，用于管理项目依赖、版本控制、项目打包，替代 GOPATH。



## 知识点
| 命令 | 作用 |
|------|------|
| `go mod init 项目名` | 初始化 Go Modules，生成 go.mod |
| `go mod tidy` | 自动清理、下载、更新依赖 |
| `go mod download` | 下载依赖到本地 |
| `go mod vendor` | 将依赖拷贝到 vendor 目录 |
| `go mod verify` | 校验依赖完整性 |
| `go list -m all` | 查看所有依赖 |



## go.mod 文件结构说明
```go
module 项目名

go 1.21

require (
    依赖包路径 版本号
)
```
- **module**：声明项目模块名
- **go**：指定 Go 版本
- **require**：项目依赖包



## 完整使用步骤
1. **创建项目文件夹**
2. **初始化模块**
```bash
go mod init myproject
```
3. **编写代码，引入第三方包**
4. **自动管理依赖**
```bash
go mod tidy
```
5. **运行项目**
```bash
go run main.go
```



## 重点总结
1. Go Modules 让项目**脱离 GOPATH**，随处可运行
2. `go mod init` 一次创建，永久使用
3. `go mod tidy` 最常用：自动增删依赖
4. 生成 `go.mod` + `go.sum` 管理依赖版本
5. 多版本、多环境、团队开发必备工具


