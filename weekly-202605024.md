# 学习周报（20260524）

# Go语言接口详解
## 一、接口基础介绍
1. 接口是**方法集合**，定义行为规范，只声明方法不实现逻辑
2. Go接口为**非侵入式**，无需关键字显式声明实现
3. 作用：统一行为、代码解耦、实现多态、提升扩展性

## 二、接口定义语法
```go
type 接口名 interface {
    方法名(入参) 返回值
}
```


## 三、接口实现规则
1. 类型拥有接口**全部方法**，即自动实现该接口
2. 方法名、参数、返回值必须完全匹配
3. 结构体、自定义类型均可实现接口

## 四、基础实现示例
```go
package main
import "fmt"

type Animal interface {
    Speak() string
}

type Dog struct{ Name string }
func (d Dog) Speak() string {
    return "汪汪汪"
}

type Cat struct{ Name string }
func (c Cat) Speak() string {
    return "喵喵喵"
}

// 接口参数，多态调用
func callSound(a Animal) {
    fmt.Println(a.Speak())
}

func main() {
    callSound(Dog{"旺财"})
    callSound(Cat{"咪咪"})
}
```

## 五、空接口
### 1. 定义
不含任何方法：`interface{}`
### 2. 特性
所有类型默认实现空接口，可存放任意数据，万能容器
### 3. 常用场景
- 函数接收任意类型参数
- 存储混合类型切片、字典
- JSON通用数据解析

### 4. 示例
```go
func printAny(v interface{}){
    fmt.Println(v)
}

func main(){
    printAny(10)
    printAny("Go接口")
    printAny(3.14)
}
```

## 六、类型断言
### 1. 作用
将空接口数据还原为原始具体类型
### 2. 基础语法
```go
// 危险写法
val := 接口变量.(目标类型)
// 安全写法
val, ok := 接口变量.(目标类型)
```
ok为true转换成功，false转换失败

### 3. 断言示例
```go
func checkType(v interface{}){
    if n,ok:=v.(int);ok{
        fmt.Println("整数：",n)
        return
    }
    if s,ok:=v.(string);ok{
        fmt.Println("字符串：",s)
        return
    }
    fmt.Println("未知类型")
}
```

## 七、类型分支 type switch
批量判断多种数据类型，简化多段断言
```go
func typeSwitch(v interface{}){
    switch v.(type) {
    case int:
        fmt.Println("整型")
    case string:
        fmt.Println("字符串")
    case bool:
        fmt.Println("布尔型")
    default:
        fmt.Println("其他类型")
    }
}
```

## 八、值接收者与指针接收者实现接口
1. **值接收者**：结构体值、结构体指针均可赋值给接口变量
2. **指针接收者**：仅结构体指针可赋值接口，原值无法匹配

## 九、核心知识汇总表
| 知识点              | 说明                             |
| ------------------- | -------------------------------- |
| 普通接口            | 约束一组方法，实现多态统一调用   |
| 空接口`interface{}` | 容纳所有数据类型                 |
| 类型断言`v.(T)`     | 接口转回原始类型，双返回值防报错 |
| type switch         | 批量区分多种数据类型             |
| 非侵入实现          | 满足方法即实现接口，无需额外声明 |

## 十、整体总结
1. 接口定义行为标准，满足全部方法即可自动实现
2. 空接口适配任意数据，常用于通用参数与容器存储
3. 类型断言、类型分支可还原接口存储的真实类型
4. 接收者形式决定接口赋值兼容范围
