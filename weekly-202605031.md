# 学习周报（20260531）



---

# 一、结构体实现**多个接口**
一个结构体可以同时实现**多个不同接口**，拥有全部行为。

## 1. 定义多个接口
```go
type Eater interface {
	Eat()
}

type Runner interface {
	Run()
}
```

## 2. 结构体实现所有接口方法
```go
type Dog struct {
	Name string
}

// 实现 Eater 接口
func (d Dog) Eat() {
	fmt.Println(d.Name, "吃东西")
}

// 实现 Runner 接口
func (d Dog) Run() {
	fmt.Println(d.Name, "跑")
}
```

## 3. 使用
```go
func main() {
	dog := Dog{"旺财"}

	var e Eater = dog
	var r Runner = dog

	e.Eat()
	r.Run()
}
```

---

# 二、接口嵌套（组合）
一个接口可以**嵌套其他接口**，拥有所有方法。

```go
type Animal interface {
	Eater
	Runner
	Speak() string
}
```
- 嵌套后，实现 Animal 必须实现：`Eat() + Run() + Speak()`
- 类似“接口的继承”

---

# 三、结构体**指针接收者**实现接口（重点）
## 1. 指针接收者
```go
func (d *Dog) Speak() string {
	return "汪汪"
}
```

## 2. 规则
- **值接收者**：结构体值、结构体指针 **都能赋值给接口**
- **指针接收者**：**只有结构体指针**能赋值给接口
  - `&Dog{}` ✅
  - `Dog{}` ❌ 报错

## 3. 为什么用指针接收者
1. 可以**修改结构体本身**
2. 大结构体**避免拷贝**，节省内存

---

# 四、空接口 `interface{}` 细节
1. **空接口可以存任意类型**：int、string、结构体、指针…
2. 空接口切片 `[]interface{}` 可存**不同类型数据**
3. 空接口作为函数参数 = **万能参数**

```go
func test(i interface{}) {}
```

---

# 五、类型断言 + 输出**结构体字段/属性**
空接口存结构体 → **断言后访问字段**

## 示例代码
```go
type Student struct {
	Name string
	Age  int
}

func main() {
	var i interface{} = Student{"小明", 18}

	// 类型断言，还原结构体
	stu, ok := i.(Student)
	if ok {
		fmt.Println(stu.Name) // 输出结构体属性
		fmt.Println(stu.Age)
	}
}
```

---

# 六、类型断言 + **指针类型结构体**
如果空接口存的是**结构体指针**：

```go
var i interface{} = &Student{"小红", 17}

stu, ok := i.(*Student) // 断言为指针
if ok {
	fmt.Println(stu.Name) // 照样访问
}
```

---

# 七、type switch（批量判断类型）
```go
func checkType(v interface{}) {
	switch v.(type) {
	case int:
		fmt.Println("int")
	case string:
		fmt.Println("string")
	case Student:
		fmt.Println("Student 结构体")
	case *Student:
		fmt.Println("*Student 指针")
	}
}
```

---

# 八、总结

| 知识点      | 说明                                     |
| ----------- | ---------------------------------------- |
| 多接口实现  | 一个结构体可实现多个接口，拥有全部方法   |
| 接口嵌套    | 接口里嵌套接口，拥有所有方法，类似继承   |
| 值接收者    | 结构体值、指针都能赋值接口               |
| 指针接收者  | 只有结构体指针能赋值接口，可修改原结构体 |
| 空接口      | 存任意类型，万能容器                     |
| 类型断言    | `v.(T)` 把空接口转回真实类型             |
| 断言结构体  | 断言成功后可直接访问 `.字段`             |
| type switch | 快速判断多种类型                         |

---
