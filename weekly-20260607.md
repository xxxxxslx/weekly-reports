# 学习周报（20260607）


---

# 一、Go 并发基础概念
1. **并发**：多个任务**交替执行**（单核切换）
2. **并行**：多个任务**同时执行**（多核）
3. Go 实现并发核心：
   - **goroutine**：轻量级线程（用户态）
   - **channel**：通道，用于 goroutine 之间通信

---

# 二、goroutine（协程）
## 1. 是什么
- 极轻量级执行单元
- 比线程小很多，可轻松开启上万 goroutine
- 由 Go 运行时调度，非操作系统调度

## 2. 开启 goroutine
在函数调用前加 **`go`** 关键字
```go
go 函数名()
```

## 3. 简单示例
```go
package main

import (
	"fmt"
	"time"
)

func printNum() {
	for i := 1; i <= 5; i++ {
		fmt.Println("num=", i)
		time.Sleep(100 * time.Millisecond)
	}
}

func printStr() {
	for i := 1; i <= 5; i++ {
		fmt.Println("str=go")
		time.Sleep(100 * time.Millisecond)
	}
}

func main() {
	go printNum() // 开启协程1
	go printStr() // 开启协程2

	// 主goroutine等待
	time.Sleep(1 * time.Second)
	fmt.Println("main结束")
}
```

## 4. 特点
- 启动极快
- 占用内存极小
- 自动利用多核实现并行
- 无需手动管理

---

# 三、channel（通道）
## 1. 作用
- 连接多个 goroutine
- **goroutine 之间通信、传数据**
- 控制同步、等待

## 2. 定义 channel
```go
通道变量 := make(chan 数据类型)
```

## 3. 发送与接收
```go
ch <- 数据   // 发送数据到通道
变量 := <-ch // 从通道接收数据
```

## 4. 简单通道示例
```go
package main

import "fmt"

// 协程：向通道发数据
func sendData(ch chan int) {
	ch <- 100
}

func main() {
	ch := make(chan int)

	go sendData(ch)

	// 主goroutine接收（会阻塞等待）
	res := <-ch
	fmt.Println("收到：", res)
}
```

---

# 四、通道的重要特性
1. **默认是阻塞的**
   - 发送时：没人接收会阻塞
   - 接收时：没人发送会阻塞
2. 通道可以实现**同步**
3. 通道必须用 `make` 创建
4. 通道是**引用类型**

---

# 五、无缓冲通道（同步通道）
```go
ch := make(chan int)
```
- 发送和接收必须**同时准备好**
- 严格同步

---

# 六、有缓冲通道
```go
ch := make(chan int, 3) // 容量3
```
- 缓冲区满之前**不阻塞**
- 满了才阻塞
- 适合**异步通信、流量控制**

---

# 七、关闭通道 + 遍历通道
```go
close(ch) // 关闭通道

// 遍历通道数据
for v := range ch {
	fmt.Println(v)
}
```

---

# 八、goroutine + channel 经典示例
```go
package main

import "fmt"

// 计算平方并发送到通道
func square(x int, ch chan int) {
	ch <- x * x
}

func main() {
	ch := make(chan int)

	go square(5, ch)
	go square(6, ch)

	// 接收2次
	fmt.Println(<-ch)
	fmt.Println(<-ch)
}
```

---

# 九、核心知识点总结
| 知识点      | 说明                                   |
| ----------- | -------------------------------------- |
| goroutine   | 轻量级协程，`go 函数()` 开启           |
| channel     | 协程通信管道，`make(chan 类型)`        |
| 发送        | `ch <- 数据`                           |
| 接收        | `<-ch`                                 |
| 无缓冲通道  | 同步，必须收发配对                     |
| 有缓冲通道  | 异步，带容量，满则阻塞                 |
| 阻塞特性    | 没人接收则发送阻塞，没人发送则接收阻塞 |
| close(chan) | 关闭通道，range 遍历必须关闭           |

---
